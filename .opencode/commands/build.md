---
description: Build project and verify compilation
---

Build the project. Install any missing dependencies, run the build, and report results.

Check:
1. Backend: dependencies installed (requirements.txt), Python imports resolve
2. Frontend: dependencies installed (package.json), TypeScript compiles, build passes
3. Linting passes if configured
4. Report any issues found
